version: "3.4"
services:
  imapbuild:
    volumes:
      - rpms:/home/centos/rpmbuild/SRPMS
      - rpm6:/home/centos/rpmbuild/RPMS
    build:
      context: .
    image: rpmbuild:6-imapbuild

volumes:
  rpms:
    name: rpms
  rpm6:
    name: rpm6